# Python
__pycache__/
*.py[cod]
*$py.class
.venv/
venv/
env/
.python-version
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/
dist/
build/

# uv
uv.lock

# Node
node_modules/
.npm/
.pnpm-store/
package-lock.json
yarn.lock
pnpm-lock.yaml

# Frontend build
frontend/dist/
frontend/.vite/
frontend/tsconfig.tsbuildinfo
frontend/**/*.tsbuildinfo

# Env + secrets
.env
.env.*
!.env.example

# OS
.DS_Store
Thumbs.db

# Editor
.idea/
.vscode/
*.swp

# NHL data cache — committed at v2 release so the backend is deploy-ready
# without running the pipeline. The parquets total only ~2 MB (zstd-compressed)
# so they're cheap to track. Use `/admin/refresh` to update post-deploy.
